using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if (Input.GetKeyDown(KeyCode.Q))
        //{
        //   BaseMemoryObj obj= MemoryPoolController.Instance.GetObjForObjType(MemoryPoolObjType.Cube);
        //    obj.OutPool(new object[] { new Vector3(1, 1, 1), new Vector3(1, 1, 1), new Vector3(1, 1, 1) });
        //}
        //if (Input.GetKeyDown(KeyCode.W))
        //{
        //    BaseMemoryObj obj = MemoryPoolController.Instance.GetObjForObjType(MemoryPoolObjType.Capsule);
        //    obj.OutPool(new object[] { new Vector3(1, 2, 1), new Vector3(1, 1, 1), new Vector3(1, 1, 1) });
        //}
        //if (Input.GetKeyDown(KeyCode.E))
        //{
        //    BaseMemoryObj obj = MemoryPoolController.Instance.GetObjForObjType(MemoryPoolObjType.Quad);
        //    obj.OutPool(new object[] { new Vector3(1, -1, 1), new Vector3(1, 1, 1), new Vector3(1, 1, 1) });
        //}
    }
}
